spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krixon/spring-ecosystem-config.git

server:
  port: 8888

---
spring:
  profiles: docker

server:
  port: ${SERVICE_PORT}