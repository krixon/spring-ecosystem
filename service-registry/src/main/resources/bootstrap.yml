spring:
  application:
    name: service-registry

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

---
spring:
  profiles: docker

eureka:
  instance:
    prefer-ip-address: true
